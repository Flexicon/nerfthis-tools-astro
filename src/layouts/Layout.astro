---
export interface Props {
  title: string;
}

const pathname = new URL(Astro.request.url).pathname;

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="generator" content={Astro.generator} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>{title}</title>

    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  </head>
  <body class="leading-8">
    <header class="py-3 px-5 bg-zinc-800 text-white">
      <nav class="flex items-center gap-5 md:w-11/12 xl:w-3/5 mx-auto">
        <a href="/" class="flex items-center text-2xl">
          <strong>🛠️&nbsp;&nbsp;NerfThis Tools</strong>
        </a>

        <div role="navigation">
          <ul class="me-auto">
            <li>
              <a
                class={["nav-link", pathname === "/ip" && "active"].join(" ")}
                href="/ip">IP Address</a
              >
            </li>
          </ul>
        </div>
      </nav>
    </header>

    <main>
      <slot />
    </main>
  </body>
</html>

<style is:global>
  .nav-link {
    @apply text-gray-400 transition-colors hover:text-gray-300;
  }

  .nav-link.active {
    @apply text-gray-100;
  }
</style>
